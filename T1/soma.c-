/** Este programa calcula a soma de dois números fornecidos 
pelo usuário */

void main(){
	int x; int y;
	int resultado;
	x = input(); y=input();

	if (x == y){
		print(int);
	}

	resultado = x+y;
	output(resultado);
}